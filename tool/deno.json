{
  "name": "@kg/tool",
  "version": "0.1.0",
  "exports": "./src/main.ts",
  "compilerOptions": {
    "noUncheckedIndexedAccess": true
  },
  "tasks": {
    "check": "deno check src/main.ts",
    "test": "deno task compile && deno test --allow-read --allow-write --allow-run --parallel src tests",
    "verify": "deno task compile && deno test --allow-read --allow-write --allow-run --parallel tests/batches",
    "ok": "deno fmt --check && deno lint && deno task check && deno task test",
    "compile": "deno compile --allow-read --allow-write --allow-run --output build/kg src/main.ts"
  },
  "imports": {
    "@std/yaml": "jsr:@std/yaml@^1",
    "@zod/zod": "jsr:@zod/zod@^4",
    "@std/uuid": "jsr:@std/uuid@^1",
    "@std/path": "jsr:@std/path@^1",
    "@std/assert": "jsr:@std/assert@^1"
  },
  "fmt": {
    "lineWidth": 90,
    "exclude": [
      "**/*.md"
    ]
  }
}
